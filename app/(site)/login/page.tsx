import AuthComponent from './components/Auth';

interface pageProps {}

const page: React.FC<pageProps> = ({}) => {
  return (
    <div className=''>
      <AuthComponent />
    </div>
  );
};

export default page;
